#include <iostream>

using namespace std;

int main() {
    double area;
    int base, height;
    
    cin >> base >> height;
    
    area = (0.5)*(base * height);
    cout << area;
    
    return 0;
}